#include <stdio.h>

int main() {
    int valor1, valor2, PROD;

    // Lendo os dois valores inteiros
    scanf("%d", &valor1);
    scanf("%d", &valor2);

    // Calculando o produto
    PROD = valor1 * valor2;

    // Imprimindo o resultado
    printf("PROD = %d\n", PROD);

    return 0;
}
